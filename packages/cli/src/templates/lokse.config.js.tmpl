/**
 * @type {import('lokse').LokseConfig}
 */
const config = {
    sheetId: "<%= sheetId %>",
    dir: "<%= outDir %>",
    languages: <%= JSON.stringify(languages) %>,
    column: "<%= column %>",
};

module.exports = config;